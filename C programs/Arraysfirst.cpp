#include<stdio.h>
int main()
{
	int number[5];
	int i;
	 for(i=0 ; i<5 ; ++i);
	 {
	 	
		printf("Enter the numbers :");
	 	scanf("%d",&number[i]);
	 	
	 }
	 for(i=0 ; i<5 ; i++)
	 {
	 	printf("%d\t",number[i]);
	 }
	return 0;
}
